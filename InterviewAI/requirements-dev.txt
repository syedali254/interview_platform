# Tooling for verifying the artefact and rebuilding the written deliverables.
#
# Kept separate from requirements.txt because none of it is needed to run an
# interview — the server, the agent and the scoring pipeline import none of
# these. run.bat installs them anyway so that a fresh checkout can run the
# tests and rebuild the report and deck without a second setup step, but a
# failure here is not fatal to the application.

# Test suite — tests/test_core.py
pytest==9.1.1

# Evaluation harness statistics and figures — experiments/run_evaluation.py
scipy==1.17.1
matplotlib==3.11.0

# Dissertation build — report/build_report.py, report/diagrams.py
python-docx==1.2.0

# Viva deck build — viva/build_viva.py
python-pptx==1.0.2
Pillow==12.3.0
